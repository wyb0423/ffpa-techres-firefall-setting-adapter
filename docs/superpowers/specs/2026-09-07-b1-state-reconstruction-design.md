# B1：国家重建与国际体系

2026-09-07。用户已批准推荐的方案 1：三项科技文本与被动效果适配。发布版本 0.2.0，延续首版五项修改。

## 设计与数值

| ID / 名称 | 效果 | 上游 → 本版 |
|---|---|---|
| international_organizations / 幸存者协定体系 | 一般恶名产生 | +10% → +5% |
| 同上 | 对未受认可国家恶名产生 | +10% → +10% |
| 同上 | 影响力 | 无 → +5% |
| decolonization / 继承政权承认 | 威望 | +25% → +10% |
| 同上 | 对未受认可国家恶名产生 | +20% → +10% |
| neoimperialism / 重建霸权 | 影响力 | +10% → +10% |
| 同上 | 警察机构成本 | -10% → -10% |
| 同上 | 一般恶名产生 | 无 → +5% |

三项科技合计：影响力 +10% → +15%，威望 +25% → +10%，一般恶名 +10% → +10%，对未受认可国家恶名的专门修正 +30% → +20%，警察机构成本 -10% → -10%。不同类型的恶名修正单独列出，不将其当作实际战争目标最终恶名。

设计将国际协作的外交收益、主权承认的声望与扩张约束、霸权经营的收益和成本分别落实到三项科技。三者可同时研究，并不构成互斥政策。数值是本轮初始平衡值，尚无实机平衡结论。说明文本用于交代制度背景，不新增援助、驻军或承认外交行动。

## 实施顺序与边界

1. 以 Firefall 0.1.1 的 `ztr_new_society.txt` 为最终上游源，核对 Tech & Res 副本。
2. 在现有科技文件增加三个完整顶层覆盖；只改数值 modifier，保留所有其他字段和三个布尔 modifier。
3. 在既有英中本地化各增加六个键，并保留 UTF-8 BOM。
4. 更新版本、README 和覆盖所有权说明。
5. 验证八个顶层对象、16 个双语键、首版对象不变、B1 非 modifier 字段与上游一致及布尔值保留。

保留 ID、时代、贴图、前置与 AI 权重；`neoimperialism` 实际仍为 era_6，不跟随其上方的 Era 7 注释改变时代。不修改其他 FFPA 包、事件、on_action、开局科技档案或生产方式。用户批准本轮后，首版文档的三项暂缓范围由本记录按方案 1 局部扩展。

## 引用与兼容证据

- `international_organizations`：和平统一外交行动的实际解锁科技；AI 策略直接读取它来压低领土扩张、将殖民扩张策略权重乘为零。保留规则，不将策略权重为零描述为禁止全部殖民。
- `decolonization`：保留非殖民化布尔 modifier；相关系统另有全局变量、游戏规则及殖民法律条件，保留这些入口。
- `neoimperialism`：核不扩散条约决议读取任意国家是否已研究它；`pm_postwar_base` 也以它为前置。均保持原行为。
- 中等开局档案已研究第一项，先进档案已研究全部三项；数值调整从这些国家开局即有影响。
- 三个布尔 modifier 保留并不证明对应系统已经运行；研究提示和相关 UI 继续沿用上游术语，本版不扩展到方案 2。
- 加载顺序保持 Tech & Res → Firefall → 本适配包。保留 ID，无新增持久变量；旧存档重算仍需实机确认。

## 验收

静态验收：元数据、八个顶层科技、B1 数值、首版对象、非 modifier 字段、三个布尔值、双语键及 BOM、Git 空白检查。

2026-09-07 已通过上述静态检查：Tech & Res 与 Firefall 的三个上游对象一致；八项覆盖集合正确；原五项脚本与十个旧语言键逐项保持一致；每种语言均有 16 个唯一名称/说明键；B1 非 modifier 字段和布尔值与上游一致。尚未启动游戏，本次没有新增运行日志证据。

实机验收：分别查看中等/先进国家开局的三项 tooltip；低科技国家研究后检查数值；在满足上游条件时检查和平统一行动及关联系统；加载已研究三项的旧存档、重存重载、禁用适配包验证研究状态；检查本轮启动日志中的目标路径和 ID。
