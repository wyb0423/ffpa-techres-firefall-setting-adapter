# CMF 接入实施顺序

1. 保存原工作树 diff；跑现有检查建立基线。
2. 接入硬依赖、独立组织分组、幂等初始化和公共总览。
3. 复用 CMF 进度组件，绑定现有投票与任务数据；加入成员／协调国展示。
4. 接入信件和窄版电报，维护中英文文本与默认字体。
5. 验证真实源码接口、只读计算边界、唯一初始化、原有机制回归、BOM／JSON／括号／键和 diff；记录实机验收缺口。

writing-plans 技能未安装；本文件直接记录已批准范围的施工顺序，不额外安装工具。
